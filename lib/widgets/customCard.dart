import 'package:flutter/material.dart';

class CustoCard extends StatelessWidget {
  const CustoCard({super.key});

  @override
  Widget build(BuildContext context, ) {
    return Card(
      color:  Colors.blue,
    );
  }
}